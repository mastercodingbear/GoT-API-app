import React from 'react';
import './Loading.css';

const Loading: React.FC = () => {
  return (
    <tr>
      <td className="loading">Loading...</td>
    </tr>
  );
};

export default Loading;
